# Orch machine-local overrides (PRD §17). Generated by
# `orch configure-local`; only the keys below apply on this machine — never
# a substitute for the committed configuration, and never itself committed.

[metrics]
enabled = true
